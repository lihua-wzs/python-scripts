# 4 周 Python 脚本训练计划

> 目标：先具备「独立写脚本」的能力，再碰 LangChain、RAG、大模型项目。
> 唯一铁律：**先自己写，跑不通再查单个 API，绝不整段复制。**

---

## 背景与现状

- 民办本科、大三、不考研，目标找 AI 相关工作。
- 现状：Python / C / C++ 只有课本基础语法，独立写完整脚本能力弱，习惯跟着教程抄代码。
- 核心判断：脚本是地基，地基没打好，直接冲大模型项目只会复制粘贴，简历项目水、面试一戳就崩。

## 调整后的路线（大三上）

```
会写独立脚本 → Python 工程能力 → C++ 巩固 → 计算机基础 → 再做 AI 应用项目
```

三道自测题（毕业考试）：
1. 写脚本读取文件夹所有文件，打印全部文件名。
2. pandas 读取 csv，按条件筛选数据，保存新 csv。
3. 写一个函数，接收列表，返回列表中的偶数。

---

## 第 1 周：文件 IO + 基础语法（把"能写出来"打通）

| 天 | 练什么 | 具体任务 | 验证标准 |
|---|---|---|---|
| Day 1 | 变量 / 输入输出 | 写脚本，输入姓名+年龄，输出"XX，你明年 N 岁" | `python hello.py` 交互输入能跑 |
| Day 2 | list + 函数 | 写 `get_even(nums)` 返回偶数列表（**自测题3**）；再写求最大/最小/平均的函数 | 传 `[1,2,3,4,5]` 返回 `[2,4]` |
| Day 3 | dict + 循环 | 给一段字符串，统计每个字符出现次数，打印 dict | 能输出正确的计数 |
| Day 4 | 读/写 txt | 读一个 txt，按行打印带行号；再写一个新 txt | 中文路径不乱码 |
| Day 5 | 遍历文件夹 | 打印某文件夹下所有文件名（**自测题1**）；进阶：只打印 `.txt` 并统计每个行数 | 换文件夹路径也能跑 |
| Day 6 | try-except | 写"输入数字做除法"，用 try-except 处理除零、非数字输入，不崩溃 | 乱输不崩、给提示 |
| Day 7 | **周测** | 独立完成三题自测，全程不看教程，写完 push GitHub | 三题全跑通 |

## 第 2 周：数据处理 + 命令行

| 天 | 练什么 | 具体任务 | 验证标准 |
|---|---|---|---|
| Day 8 | csv 模块 | 用标准库 `csv` 读 csv，打印表头和前几行 | 中文不乱码 |
| Day 9 | csv 写入 | 读 csv，把"分数≥60"的行写进新 csv（先用标准库实现） | 新文件行数正确 |
| Day 10 | pandas 读 | `read_csv`，打印 `info()`/`describe()`/前5行 | 看得懂输出 |
| Day 11 | pandas 筛选导出 | 用 pandas 实现"过滤低于60分另存"（**自测题2**）；再练多条件筛选 + `groupby` 分组统计 | 独立写出来 |
| Day 12 | numpy 基础 | 创建 array，切片、运算、`mean/max` | 会基本操作 |
| Day 13 | 命令行参数 | 用 `sys.argv` 和 `argparse` 写"接收文件名当参数"，`python script.py data.csv` 能传参 | 参数能传进去 |
| Day 14 | **周测** | 写 `python filter.py data.csv 60`，过滤低于指定分的行，存成 out.csv（阈值可传参） | 换个阈值也能跑 |

## 第 3 周：工程能力 + 读报错（很多人卡在这）

| 天 | 练什么 | 具体任务 | 验证标准 |
|---|---|---|---|
| Day 15 | 虚拟环境 | 建 `venv`、激活、`pip install pandas` | 能进能出环境 |
| Day 16 | 依赖管理 | `pip freeze` 导出 requirements.txt；删环境后用 requirements 重装一遍 | 能一键还原 |
| Day 17 | Git | init/add/commit/push，把前两周脚本推 GitHub，README 写清每个脚本干啥 | 主页能看到提交记录 |
| Day 18 | **读报错专项** | 故意制造：路径不存在、类型错、KeyError、库没装，练从 Traceback **最后一行往上**定位 | 能说清每类错在哪 |
| Day 19 | 批量重命名 | 给文件夹所有文件加前缀，先 dry-run 打印"将要改名"，再真正执行 | 不会误改文件 |
| Day 20 | 合并 csv | 把一个文件夹里所有 csv 合并成一个总表 | 列能对齐 |
| Day 21 | 复盘提交 | 本周脚本全 push，回头读一遍自己写的代码 | 能讲出每行干啥 |

## 第 4 周：综合小项目 + 验收

| 天 | 练什么 | 具体任务 |
|---|---|---|
| Day 22 | 日志分析 | 读日志文件，统计 ERROR / WARN 出现次数 |
| Day 23 | 数据 pipeline | 读csv → 去空值/去重 → 筛选 → 统计 → 导出，一条流水线写成一个脚本 |
| Day 24 | **毕业自测** | 三题自测 + 一道综合题："统计文件夹里所有 txt 的总行数和总字数"，全程独立 |
| Day 25-28 | 补漏缓冲 | 把卡壳的重做，全 push，整理成 GitHub 项目（加 README） |

---

## 每天的执行模板（比清单本身更重要）

1. **关教程**，只看题目，先自己写（哪怕写得烂）。
2. **跑**，看报错。
3. 从 Traceback **最后一行往上**定位，改，再跑，直到跑通。
4. 跑通就 `git commit`。

> 允许查官方文档、搜索引擎查**单个 API**；**不允许复制整段完整代码**。二者区别就是"你会不会"的分界线。

## 何时算过关、可以进第二阶段

能**不看教程**独立完成三题自测 + "csv 过滤"那道题，就说明脚本能力达标了。
这时候再去碰 FastAPI、调用 DeepSeek/通义 API、做 RAG，才接得住。没达标就继续在第 1 阶段磨，别跳。

---

## 时间分配（大三上）

| 板块 | 占比 |
|---|---|
| Python 脚本实战训练 | 40%（重中之重） |
| C++ + 算法刷题 | 20% |
| 计算机基础 + MySQL | 20% |
| Git、工具环境 | 10% |
| AI 框架 / 项目 | 10%（前期少，脚本练好再加） |
